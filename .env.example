VITE_API_URL=https://hybrid-vector-api.onrender.com
VITE_TENANT_ID=workguard-demo
VITE_HV_API_KEY=workguard-key-2026

# /auth-payment — composite score threshold for grant disbursement approval.
# composite >= threshold     → APPROVED
# composite >= 0.60          → REVIEW (human agent)
# composite <  0.60          → REJECTED (retry, max 3, then MANUAL_REVIEW)
VITE_PAYMENT_THRESHOLD=0.75
